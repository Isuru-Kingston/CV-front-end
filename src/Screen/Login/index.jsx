import React, { useState } from "react";
import LoginPanel from "../../Features/LoginPanel";

const Login = () => {
  return (
    <div>
      <LoginPanel />
    </div>
  );
};

export default Login;
